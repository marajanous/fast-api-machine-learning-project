# Project tasks

1. Install WSL
2. Install Docker desktop
   -> Let me know when you are done here
3. (Michal) create Docker compose file for NoSQL database and MiniIO
4. Prepare .gitignore (AI), precommit actions, linter, mypy..
5. Prepare basic FastAPI structure (validation of input, definition of login/logout/me (info about user) endpoint, proper logging)
